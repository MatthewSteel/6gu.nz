{
  "name": "sheet-monorepo",
  "version": "0.1.0",
  "private": true,
  "dependencies": {},
  "scripts": {
    "dev": "cd client && yarn run start",
    "build": "cd client && react-scripts build",
    "test": "cd client && react-scripts test --env=jsdom"
  },
  "devDependencies": {
    "eslint": "^4.19.1"
  },
  "license": "UNLICENSED"
}
